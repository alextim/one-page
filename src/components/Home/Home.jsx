import React from 'react';
import Menu from '../Menu';

const menuItems = {
  Top: 0,
  Section1: null,
  Section2: null,
  Section3: null,
};

const Home = () => {
  return <Menu menuItems={menuItems} />;
};

export default Home;
